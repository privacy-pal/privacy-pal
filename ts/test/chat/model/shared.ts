export enum FirestoreCollections {
    Users = "users",
    GroupChat = "gcs",
    DirectMessages = "dms",
    Messages = "messages",
}

export enum JoinQuitAction {
    JoinChat = 'join',
    QuitChat = 'quit',
}

export const doesNotExistError = "does not exist";